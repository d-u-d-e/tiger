(
let
  type
  type T
  type T = 
  type T = {}
  type T = {
  type T = {a}
  type T = {a: }
  type T = {a: int, b: int
in
end;

let
  type A = array
  type A = array of
  type A = array of int
in
end
)